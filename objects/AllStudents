package objects;

import java.util.ArrayList;

public class AllStudents {
	private ArrayList<StudentInfo> students = new ArrayList<StudentInfo>();
	private ArrayList<StudentInfo> gradeSortedStudents = new ArrayList<StudentInfo>();
	private ArrayList<StudentInfo> nameSortedStudents = new ArrayList<StudentInfo>();
	
	public void addStudent(String name, double homeworkAverage, double[] tests, double[] quizzes) {
		ArrayList<Double> testNums = new ArrayList<Double>();
		ArrayList<Double> quizNums = new ArrayList<Double>();
		
		students.add(new StudentInfo(name, homeworkAverage, testNums, quizNums));
		gradeSort();
		nameSort();
	}
	
	public ArrayList<StudentInfo> getStudents() {
		return students;
	}
	
	private void gradeSort() {
		gradeSortedStudents = students;
		for(int i = 0; i < gradeSortedStudents.size() - 1; i++){
			int maxIndex = i;
			for(int j = i + 1; j < gradeSortedStudents.size(); j++){
				if(gradeSortedStudents.get(j).getFinalAverage() > gradeSortedStudents
			}
		}
	}
	
	private void nameSort() {
		
	}
}
